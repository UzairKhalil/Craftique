/**
 * Generated types — DO NOT EDIT BY HAND.
 *
 * PROJECT_PLAN §9.5: the shapes crossing the PHP/TypeScript boundary (DTOs,
 * API Resources, enums) are generated from their PHP definitions so the
 * contract cannot silently drift. Hand-written app types belong in
 * `index.d.ts`; anything mirroring a PHP class belongs here.
 *
 * Generation is wired up once the first DTOs and enums exist (M2 onward).
 * Until then this file only declares the marker below so imports of
 * `@/types/generated` resolve and the file is never accidentally hand-edited.
 */

/** Replaced by the generator on first run. */
export type GeneratedTypesPlaceholder = never;
